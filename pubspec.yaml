name: expert_chat
description: "跨平台 Flutter LLM 客户端：支持任意 OpenAI 兼容 API（DeepSeek/Grok/Kimi/OpenAI/智谱等），含深度思考、联网搜索与本地文件解析。"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# build-number must stay > any prior split-per-abi code (abi*1000 + old build).
# v3.0.0 arm64 was 2015, x86_64 was 4015 — use 4100+ for clean upgrades.
version: 3.0.1+4100

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # Grapheme-cluster-safe string truncation (titles, clipped search text).
  characters: ^1.4.0
  flutter_riverpod: ^3.3.1
  dio: ^5.9.2
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  path_provider: ^2.1.5
  uuid: ^4.5.3
  gpt_markdown: ^1.1.7

  # M3 — markdown / links
  url_launcher: ^6.3.2
  # M2 — persistence (drift / SQLite). drift_flutter bundles sqlite3 native libs.
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  # M4 — file parsing (pure-Dart). Keep archive 3.x / xml 6.x — newer majors
  # break syncfusion/excel/docx_to_text resolution.
  syncfusion_flutter_pdf: ^33.2.8
  docx_to_text: ^1.0.1
  excel: ^4.0.6
  archive: ^3.6.1
  xml: ^6.5.0
  file_picker: ^11.0.2
  mime: ^2.0.0
  # M5 — web search (call search APIs via dio; client-side article extraction)
  html: ^0.15.6
  html2md: ^1.3.2
  # M6 — share (mobile share sheet; desktop uses file_picker.saveFile).
  # Pinned to 12.x: share_plus 13.x needs win32 ^6 which conflicts with
  # file_picker 11.x (win32 ^5).
  share_plus: ^12.0.2
  # App version + GitHub Releases update check
  package_info_plus: ^8.3.0
  crypto: ^3.0.6
  # Shorebird code-push (Dart OTA patches; requires Shorebird-built release)
  shorebird_code_push: ^2.0.7
  webview_flutter: ^4.14.1
  # Browser blob URLs for sandboxed HTML preview.
  web: ^1.1.1
  flutter_local_notifications: ^22.2.0
  wakelock_plus: ^1.3.3
  # Short-phrase dictation through the device's system speech recognizer.
  speech_to_text: ^7.4.0
  # Captures a WAV file for the configured cloud ASR service (including MiMo).
  record: ^7.1.1
  # Plays OpenAI-compatible /audio/speech responses. Windows needs its
  # platform implementation as a direct dependency.
  just_audio: ^0.10.6
  just_audio_windows: ^0.2.3
  # M7 research mode (native SSH; web uses stubs)
  dartssh2: ^2.22.3
  xterm: ^4.0.0
  photo_manager: ^3.11.0

# Stock speech_to_text_windows 1.0.1 never calls SAPI SetInterest, so listen
# appears to start but never yields transcripts. Use our patched copy.
dependency_overrides:
  speech_to_text_windows:
    path: third_party/speech_to_text_windows

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Tests install the package's fake platform implementation directly.
  flutter_secure_storage_platform_interface: ^2.0.1
  # Tests install the package's fake platform implementation directly.
  path_provider_platform_interface: ^2.1.2
  # TTS playback tests fake the just_audio platform via this interface.
  just_audio_platform_interface: ^4.6.0
  # M2 — drift codegen (keep drift_dev on the same minor as drift)
  drift_dev: ^2.33.0
  build_runner: ^2.15.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled UI font: Noto Sans SC (variable weight) — set as the global default
  # in AppTheme so all text (Latin + CJK) is one consistent, high-quality face
  # across every platform.
  fonts:
    - family: NotoSansSC
      fonts:
        - asset: assets/fonts/NotoSansSC.ttf
  assets:
    - shorebird.yaml
    # Short Edge-TTS samples used as optional clone seeds for anime-style
    # voice packs (archetypes, not copyrighted character rips).
    - assets/tts_voices/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
