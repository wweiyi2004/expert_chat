# Copy to mcp.env next to expert-chat-mcp (or export these variables).
MCP_API_TOKEN=change-me
MCP_HOST=127.0.0.1
MCP_PORT=8790
MCP_DATA_DIR=./mcp_data
# Public installs should be HTTPS, e.g. https://mcp.example.com/mcp
MCP_PUBLIC_URL=http://127.0.0.1:8790/mcp
